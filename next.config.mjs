/** @type {import('next').NextConfig} */
    const nextConfig = {
      output: 'export',
      basePath: '/PortImpl',
      images: {
        unoptimized: true,
      },
    };

    export default nextConfig;